%h2 Create New Sprints

-# where form will be posted, loopup rake routes
= form_tag sprints_path, :method => :post do
	= render :partial => 'form'
	= submit_tag 'Save'
-# path to the old sprints. Assignment is finished before any action. 
- my_path = sprints_path
- if @sprint
	= @sprint.inspect
	- if @sprint.new_record?
		- my_path = sprints_path
	- else
		- my_path = sprint_path(@sprint)
-# generate a form. the button submits to the URL created by the set of options
= button_to 'Cancel', my_path, :method => :get
